# my-expense-app
A personal expense management web application built with Python and Streamlit. Users can register, login, add, edit, delete, filter expenses by category and date, view interactive charts (pie, line, bar), switch between dark/light mode, and export reports. All data is stored in SQLite database with secure password hashing using bcrypt.
